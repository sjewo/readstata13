# .travis.yml for readstata13.
language: r
cache: packages

r:
  - oldrel
  - devel

matrix:
  include:
    - os: linux
      r: release
    - os: osx
      r: release

notifications:
email:
on_success: change
on_failure: change
