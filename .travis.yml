# .travis.yml for readstata13.
#
# https://github.com/craigcitro/r-travis/wiki/Porting-to-native-R-support-in-Travis
language: r

matrix:
  include:
    - dist: trusty
   
r:
  - oldrel
  - release
  - devel
  
sudo: false
cache: packages

notifications:
email:
on_success: change
on_failure: change
