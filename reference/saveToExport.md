# Check if numeric vector can be expressed as integer vector

Compression can reduce numeric vectors as integers if the vector does
only contain integer type data.

## Usage

``` r
saveToExport(x)
```

## Arguments

- x:

  vector of data frame
